#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,x,i,sum=0;
    cin>>n;
    for(i=1;i<=n;i++)
    {
        cin>>x;
        sum+=x;
    }
    if(sum==2*(n-1))
    cout<<"Yes"<<endl;
    else cout<<"No"<<endl;
}
